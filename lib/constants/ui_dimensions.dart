class UIDimensions {
  // Leaderboard + PunterSelection shared widths
  static const double rankColumnWidth = 28.0;          // "P" column
  static const double punterNameColumnWidth = 68.0;    // name column
  static const double totalColumnWidth = 36.0;         // "T" column

  // Shared row heights
  static const double headerHeight = 26.0;
  static const double rowHeight = 34.0;
}