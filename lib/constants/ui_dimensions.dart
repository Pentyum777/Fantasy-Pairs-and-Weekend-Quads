class UIDimensions {
  // Leaderboard + Punter table layout
  static const double rankColumnWidth = 32;
  static const double punterNameColumnWidth = 72;  // ~12 chars
  static const double totalColumnWidth = 40;       // 3-digit total

  // NEW: Separate widths for pick and score columns
  static const double pickColumnWidth = 115;       // adjust as needed
  static const double pickScoreColumnWidth = 36;   // narrower score cell

  // Shared row heights
  static const double headerHeight = 26.0;
  static const double rowHeight = 34.0;

  // Collapsed leaderboard width
  static const double collapsedLeaderboardWidth = 40;
}