[build]
builder = "node"

[variables]
NIXPACKS_USE_NIX = "1"

[phases.setup]
nixPkgs = ["nodejs"]

[phases.setup.apt]
packages = []

[phases.install]
cmds = ["npm ci"]

[phases.start]
cmd = "npm start"